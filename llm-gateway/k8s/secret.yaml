apiVersion: v1
kind: Secret
metadata:
  name: llm-gateway-secret
  namespace: llm-gateway
type: Opaque
stringData:
  # API key for authentication (base64 encoded in production)
  # To create: echo -n "your-api-key-here" | base64
  API_KEY: "change-me-in-production"
